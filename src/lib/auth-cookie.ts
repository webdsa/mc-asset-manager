/** HttpOnly session cookie set by `/api/auth/session` (Firebase Admin session JWT). */
export const AUTH_SESSION_COOKIE = "studio_session";

export const AUTH_SESSION_MAX_AGE_SEC = 60 * 60 * 24 * 5; // 5 days
